query transactionList($limit: Int = 10, $offset: Int = 0) {
  transactionList(pagination: {limit: $limit, offset: $offset}) {
    id
    created
    amount
    user {
      id
      firstName
      lastName
      profile {
        id
        balance
      }
    }
  }
}